{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "main",
            "type": "shell",
            "command": "clear && zig run main.zig | ffplay -i pipe: -f rawvideo -video_size 1920x1080 -pixel_format bgra",
            "problemMatcher": []
        },
        {
            "label": "mainIntersection",
            "type": "shell",
            "command": "clear && zig run mainIntersection.zig | ffplay -i pipe: -f rawvideo -video_size 1920x1080 -pixel_format bgra",
            "problemMatcher": []
        },
        {
            "label": "record",
            "type": "shell",
            "command": "rm out.mp4 && clear && zig run mainIntersection.zig | ffmpeg -f rawvideo -video_size 1920x1080 -pixel_format bgra -i - out.mp4 -c:v libx264 -pix_fmt yuv420p -profile:v low",
            "problemMatcher": []
        },
        {
            "label": "x11",
            "type": "shell",
            "command": "clear && zig run x11.zig -lc -lxcb -lxcb-image",
            "problemMatcher": []
        },
        {
            "label": "window",
            "type": "shell",
            "command": "clear && zig run window.zig",
            "problemMatcher": []
        },
        {
            "label": "window linux",
            "type": "shell",
            "command": "clear && zig run window.zig -lc -lxcb -lxcb-image",
            "problemMatcher": []
        },
        {
            "label": "test",
            "type": "shell",
            "command": "clear && zig test src/index.zig",
            "problemMatcher": []
        },
    ]
}